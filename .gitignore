node_modules/
dist/
dist-electron/
*.log
.worktrees/
studio/.git/
studio/node_modules/
studio/pi-chat/node_modules/
studio/pi-chat/dist/
studio/pi-chat/src/chat-dist.html
studio/pi-chat/src/model-icons-data.ts
# artifact of the vendored pi-chat build (upstream VS Code extension copy step)
studio/src/
src/shared/*.js
src/shared/*.js.map

# Screenshots are regenerated per machine: they show real session names and
# paths from whoever ran the screenshot harness. Never commit them.
docs/screenshots/

# Release-only artifacts: attached to a GitHub Release, never committed.
pi-heao-gui-*-source.zip

# scratch files — never commit these (a `git add -A` swept some in once)
_*
scripts/_*
*.local
!._*
