# Changelog

本项目的所有重要变更记录在此。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### Added

- **打开一个仓库不再等于自动执行它的 `.pi/mcp.json`。** 那个文件本质上是一行行**命令**：会话一开在这个目录里，
  里面的每个 MCP server 就以你的权限被启动。pi 自己的项目信任门管的是 `settings.json`/`extensions`/`skills`/
  `prompts`/`themes`/system-prompt 文件 —— **偏偏不管 `mcp.json`**，所以这份文件一直是“克隆下来就生效”。
  现在首次在某个项目里遇到项目级 server 时会弹一次确认（列出 server 名字与目录），选「信任并记住」后按
  **目录 + 文件哈希**记住（文件被改/被 pull 过会重问）；不答或不信任就**不启动**，并在会话里说清楚跳过了什么。
  它不阻塞 pi 的启动：问题走会话 UI 通道，而那个通道要等 pi 启动完成才开始读（两件事都等对方就会死锁）。

### Fixed

- **便携版不再去下载“安装包版”的自动更新。** 便携版与安装版共用同一个 release 通道，`latest.yml` 里只有 NSIS
  安装程序，而 electron-updater 没有便携版分支 —— 也就是说点“检查更新”会下个安装包下来（装完是第二个副本，
  而不是替换正在跑的那个文件）。现在便携版（认 `PORTABLE_EXECUTABLE_FILE`，这是 electron-builder 的便携启动器
  唯一留下的痕迹）一律报告“请从 Releases 下载新的 Portable.exe”，不请求、不下载、也不安装。
- **诊断包里不再捎带明文密钥。** 那份文件是给人贴到公开 issue 上的（SECURITY.md 就是这么说的），而它包含 RPC
  日志：里面有 pi 的 stderr 和每条子进程的完整命令行。config 的 `env` 值此前已掩码，现在日志与信息文本再过一道
  `redactSecrets()` —— 只挡**已知的密钥形状**（`sk-`/`AIza`/`ghp_`/`npm_`/`xai-`/`hf_`/`xox*`、`Bearer …`、
  `token=`/`password=` 这类键值、足够长的裸 base64），路径、哈希、行号、exit code 全部原样保留：
  掩得太宽，诊断包就白生成了。
- **MCP 请求不再能永久挂住一个回合。** 此前只有 `connect` 有 30s 超时；`callTool` / `readResource` / `getPrompt`
  完全没有 —— 一个接受了连接却不再回包的 server，会让工具永远不返回，而那就是“回合永不结束”。“生成提交信息”那类
  长操作也不是不可能，所以上限给得宽松（120s），但必须存在。
- **`mcp.json` 里写错的 url 现在报一句人话**（哪个 server、值是什么），而不是让 `new URL` 的 TypeError 冒上去。
- **改动很大时“生成提交信息”不再报一个看不懂的 spawn 错误。** 提交信息的 prompt 是走 **argv** 传给 pi 的
  （`-p <diff> --system-prompt <text>`），而 Windows 的整条命令行上限是 32767 字符（npm 装的 `pi` 是 `.cmd`，
  经 cmd.exe 时更是 8191）—— 而原来给 diff 的预算就是 64KB，**单它一项就已经超了**，所以“正好被截断的那批大改动”
  必然失败。现在预算是整条 prompt 的 24000 字符（给可执行文件路径、参数、system prompt 都留了位置），
  开发者备注与自定义 system prompt 也各自封顶，而不是吃掉 diff 的空间；万一仍有东西溢出去，报一句人话
  （“改动太大，请分次提交”）而不是 `spawn EINVAL`。
- **diff 窗口不再是一个“多大都读”的任意文件读取口。** `pi:show-diff` / `pi:rewind-diff` 的 `absPath` 直接进
  `readFile`，读多少都行；快照的两个参数（`sessionId` / `baselineHash`，都来自渲染进程）也是原样 `join` 到
  `~/.pi/snapshots/…`，`..` 可以走到快照根之外随便读。而那个看上去像“上限”的 `MAX_HTML` 是在
  **写盘之后**才判的，实际效果只有一行日志 —— 一个 500MB 的文件足以让主进程整读、拼出几倍大的 HTML、写进临时目录。
  现在：两个快照参数必须是普通名字（拒绝 `.`/`..`/分隔符/结尾点空格，拒绝非字符串），读取前先 `stat` 验大小（8MB），
  渲染后、写盘前再验一次 HTML 尺寸，超了一律拒绝并给出原因。
- **导入会话也先看大小。** `pi:import-session` 的路径来自渲染进程（不走对话框，就是为了让 e2e 能驱动同一段代码），
  而 `readFile` 没有上限：机器上任意文件都会被整读进主进程。现在先 `stat`（要求是文件、默认 64MB 上限），再读。

- **pi 死掉时连发两条消息，不会再留下一个改不了也看不见的 pi 进程。** `reloadSession` 没有重入保护：两条消息
  在 `rpcAlive === false` 时都会各启动一个 pi，后者覆盖 `rpc` 引用而前者没人 dispose —— 它不在
  `windowSessions` 里，所以连退出也扫不到它（活到系统重启），两个进程又在往同一个会话文件里写。现在重载是
  一个共享的 in-flight Promise：并发的调用者等同一次重启，而不是各自开一个。关闭窗口时也在重启完成后补一次
  检查，避免 “重启完才发现窗口没了” 这种孤儿。
- **子代理不再能无限期挂着。** `subagent` 工具 spawn 第二个 pi，而它此前**只有一个出口：调用方的 abort** ——
  子 pi 一旦卡住（网络、或这个项目历史上真发生过的扩展事件循环泄漏），父会话的回合就永远不会 settle，聊天窗
  永久停在 streaming，只能杀应用。现在有 15 分钟的总超时（`PI_SUBAGENT_TIMEOUT_MS` 可改），超时杀掉整棵进程树，
  并把原因写回结果（`stopReason: "timeout"`），而不是只留一个 exit code 1。
- **超时的 `pi install` 不再把 npm 子进程留在后台。** `runPiCli` 超时只调 `proc.kill()`，而 Windows 上 pi 通常是
  一个 npm `.cmd`：真正在干活的是 `cmd.exe` 下的 node/npm 孙进程，杀外壳等于没杀 —— 它会继续往 `~/.pi/agent/npm`
  里装，正是 docs/KNOWN-ISSUES.md 里那类“半个树”失败。现在杀整棵树（`taskkill /T /F`，与 RPC client 早就有的
  做法对齐），子代理的 abort/超时也走同一套。新单测用一个会 spawn 孙进程、然后挂住的假 pi 验证孙进程真的没了
  （Windows 上跑，其他平台 skip）。
- **终端的会话文件不再是一条可以递给 pi 的任意路径。** `pi:term-open` 的 `sessionFile` 直接变成 pi 的
  `--session <path>` 参数，而 `pi:switch-session` 对同一个值是有校验的 —— 这里是漏的，于是渲染进程可以要求
  终端把机器上任意一个文件当会话恢复/追加。现在与切换会话用同一道栅栏（必须在 `~/.pi/agent/sessions` 下的
  `.jsonl`），不合格直接拒绝并记日志；应用自己的 dock 本来就不传这个参数，所以正常使用无影响。
- **窗口在等队列的这几分钟里被关掉，不会再留下一个没人能关的终端进程。** 「终端要等另一个 pi 启动完成」最长
  五分钟，期间关窗口时，PTY（一个 pi TUI 或一个 shell）会以已销毁的 webContents id 留在 `terminals` 里活到应用
  退出：渲染层看不到它、也无法关它。现在等之前、拿句柄之后都会检查窗口是否还在，不在了就不启动（或立即 kill）。
- **“危险命令需确认”此前是一条空转的规则 —— 现在它真的拦得住。** 设置里默认就是 `AskForApproval`，但规则表
  默认是**空的**，而权限门拿到空表就等于“一条都不匹配” → 每条 bash 命令都放行；设置界面里又根本没有编辑规则表的
  地方，所以这个默认值无从改起。现在应用**自带 39 条默认规则**（就是上游 pi-agent-studio 那套，逐字对齐，有单测钉住），
  设置 → 权限 里可以直接编辑、一键「恢复默认」；`config.json` 里**缺少**这个键时回落到默认表，**显式写成 `[]`** 才算
  “我自己要关掉它”（此前两种情况长得一样）。规则哪里无效会在会话里提示，而不是静默失效；
  `/permission` 不带参数会报出当前模式、生效规则条数与是否拦截工作区外的写入。
- **权限门不再只管 bash，也不再只管主办。** 两条同源的绕过都被堵上：（1）`write`/`edit` 的路径解析后落在**会话工作目录之外**
  时要确认（按工作目录算，`..` 会被 resolve，有 junction 时按真实路径比较 —— 这正是「它只改了仓库」变成
  改写 `~/.pi/agent/settings.json` 的那条路）；（2）`subagent` 工具的**子 pi 此前不挂权限门**（宿主只给自己的会话挂 `-e`），
  也就是把危险命令交给子代理做就绕过了全部确认 —— 现在子代理会挂上和主人同一个门（宿主通过新增的
  `PI_VSCODE_BRIDGE_DIR` 告知桥接目录），找不到门时会在日志里明说而不是静默降级。
- 权限门的判定逻辑抽成 `studio/bridge/permission-policy.mjs`（不依赖 pi，可单测）：以前它只能在一个真实会话里验证 ——
  本次审计里它恰好是最需要证据的一块，现在有 12 个单测覆盖（空规则表与默认表、大小写、无效正则、工作目录内外、
  `..`、junction、未知工具）。

### Changed

- **终端现在是 CI 里真的会被加载一次的东西。** CI 里没有任何一步会 `require("node-pty")`：`smoke` 开真窗口
  但不碰终端（全文没有 terminal 引用），`check-package` 只断言文件在 asar 里“存在”。于是“原生模块在 Electron
  下装不起来”这种事可以一路绿灯发出去，表现形式就是用户看到的“终端面板一片空白”。新增 `npm run check:pty`
  （Electron 下 spawn 一个 ConPTY、要求它回显一行），作为 `check` 作业里的**阻塞**步骤。
- **`studio/pi-chat/package-lock.json` 重新入库。** 打包用的 vendored UI 依赖此前每次 `npm install` 都可以
  漂移（lock 被 .gitignore 掉了），也就是说同一个 tag 两次构建可以不一样；现在锁定版本进库。
- **文件面板默认的工作目录就是家目录，而“在工作目录内”过去等于“整个用户目录”。** 于是聊天窗口 —— 那个专门渲染
  模型输出、被明确定义为“不能碰 agent 配置”的窗口 —— 可以通过 `pi:fs-read` 读 `~/.pi/agent/auth.json`、通过
  `pi:fs-write` 改写 `~/.pi/standalone/config.json`（下一轮 pi 启动就按里面的 `env`/`args` 跑），
  与 SECURITY.md 的承诺相反。现在 `pi:fs-tree/read/media/write` 一律拒绝受保护位置：`~/.pi`（会话、快照、
  settings/auth、应用配置、pi 装的扩展包）、`~/.ssh`、`~/.aws`、`~/.gnupg`、`~/.docker`、`~/.config`、
  `~/.npmrc`、`~/.git-credentials`、`~/.gitconfig`（git 的 `core.sshCommand` 就是一个命令执行入口），
  以及 `%APPDATA%`（`userData/bridge-extracted/*.ts` 是每个 pi 都拿 `-e` 执行的，Startup 目录也在里面）。
  判断交给新的 `isWithin()`：它与 `safeWorkspacePath()` 故意相反，**文本命中或真实路径命中任一条即算命中** ——
  工作区里指向 `~/.pi` 的 junction 必须被拦下，而“只拦两种写法都一致的那一个”恰好放过这个故事的主角。
  仓库自己的 `.pi/`（settings、mcp.json）**不在**名单里：那是用户的仓库，不是 agent 状态。
  `pi:open-file`（交给系统默认程序打开）同样过这张名单：否则它就成了绕过文件面板那道门的另一条路。
- `SECURITY.md` 里关于权限与文件面板的两段描述改成与实现一致：默认规则表、工作区外的写入确认、
  `pi:fs-*` 拒绝 `~/.pi` 等位置、以及“克隆来的仓库可以通过 `.pi/mcp.json` 跑代码”（pi 自己的项目信任门并不覆盖 `mcp.json`）。

## 1.3.0 — 2026-09-19

### Added

- **正在跑的时候点「新建会话」，现在给你一个新窗口，而不是一句拒绝。** 此前会弹「请先停止 agent 再新建会话」
  —— 因为 pi 一个进程只有一个会话，运行中的回合不允许被换掉。但“这件事先跑着，我另开一件”正是多窗口存在的
  理由，所以现在忙碌的窗口把新会话开在**它自己的窗口**里，原回合继续跑（daily e2e 断言：新窗口真的出现、
  里面是空会话、原窗口的对话没被换掉）。
- 顺带把这条功能的入口收敛成**一个共用函数**：侧栏按钮、标题栏「+」、命令面板走 IPC，**文件 → 新建会话
  （Ctrl+N）与托盘 → 新建会话此前是死的** —— 前者发出的 `newSessionShortcut` 事件无人接收，后者发到渲染层的
  消息类型根本不在投递表里，点了没有任何反应；现在它们与新会话走同一条路（忙碌时同样开新窗口）。
- **「待你处理」：所有窗口正在等你回答的事，集中在一处。** pi 需要回答时（权限、确认、输入）应用本来就会响
  提示音 —— 但**响的是哪一件事、在哪个窗口**，没有任何地方能看到。现在标题栏多一个铃铛：有几件在等就显示几，
  点开是一份**跨窗口**的清单（每条写清是什么、哪个窗口/会话、等了多久），点一条就把提出它的窗口提到前台 ——
  回答仍然在**那个窗口**里做（请求、选项、输入框都在那儿），答完条目自己消失。
  条目随会话生命周期收尾：窗口关闭、切换会话、pi 退出都会把它撤下来 —— 留在列表上等于把人指去一个已经不存在
  的窗口。子窗口现在用**同一份 host**（此前子窗口自带一份 `postToRenderer` 字面量，于是它的提问既不上榜、
  也不响提示音）。诊断包里现在也带上待处理条数与 pi 启动队列的状态。
  e2e（隔离档）：假 pi 发一条 `confirm` → 子窗口弹出对话框 → **主窗口**的铃铛变成 1 → 面板列出它 →
  点击后断言那个窗口真的获得焦点 → 在子窗口点「是」→ 条目消失。
- **一键「新副本」：建一个 worktree，并在新窗口里开始一个会话。** 变更面板里选分支、点「＋ 新副本」，就会在仓库
  旁边建出 `仓库名-分支名` 的工作副本，并开一个**自己的窗口**（标题就是分支名）在其中启动 pi —— 原窗口继续跑自己的事。
  这是“同一仓库并行推进两件事”缺的最后一块：切换与隔离早就有，缺的只是**分派**；而想要第二个工作副本的场合，
  正是当前窗口正忙的时候。分支名只做校验不做转义（git 不经过 shell），git 自己的规则（不能以 `-` 开头、不能有
  `~ ^ : ? * [ \ .. @{`）与长度都在单测里；工作副本放在仓库**旁边**而不是里面（放在里面就是自己树里的未跟踪内容，
  每次 `git status` 都会提到它）。
  e2e（隔离档）：面板里输入分支名 → 一键建出副本并开会话 → 断言副本在磁盘上、窗口标题是分支名、
  **那个窗口的 pi 真的以新目录为 cwd 启动**（假 pi 自己记下被启动的位置）、面板下拉里也列出了它。
- **导入会话：导出有了另一半。** 侧栏「导入」选一个 `.jsonl`（别的机器、别的 profile 导出的会话），它就进会话列表、
  点开就是那段对话。检查的是 pi 自己的会话头（第一行必须是会话头，否则侧栏就会列出一堆垃圾）；
  跨机器时那个会话记录的**工作目录多半不在这台机器上** —— 这时只改会话头里的 `cwd` 指向当前工作目录，**对话本身逐字节不动**，
  并在提示里说清楚。落点用 pi 自己的目录命名（`--E--AI-repo--`，拿真实 profile 的目录核对过），同名不覆盖而是加序号 ——
  同一个会话从另一个 profile 导出过来时文件名是一样的，覆盖等于把本机那份毁掉。
  e2e（隔离档）：造一个别的机器上的会话文件 → 导入 → 断言它进了列表、能打开并显示那段对话；
  再拿一个不是会话的文件导入，断言被拒且给出原因。
- **多项目：把常用的目录存下来，一键切换。** 工作目录那一条点开就是**项目列表**（名字 + 路径，当前项目打勾），
  下面接“最近使用”与“添加项目…”；每个项目可以改名、可以移出列表（**目录不会被删**，目录不在了的行会标灰）。
  侧栏搜索框旁边多一个 **「项目 / 时间」** 分组开关，按项目分组时会话按它运行时的目录归位 —— 会话头里记的就是那个
  目录，所以**开在子目录里的会话也归到它所属的项目**，嵌套项目优先于外层项目。
  项目的定义写进了 docs/ROADMAP.md：**项目 = 一个目录 + 一个名字**，不是新容器 —— 窗口仍然只有一个 cwd，
  会话、终端、git 面板、文件树全部沿用既有链路（切换项目 = 切工作目录，其余照旧跟随）。
- **窗口总览：一眼看清每个窗口在干什么。** 多开几个窗口之后，“谁在跑、谁在等你、谁闲着”此前只能靠托盘菜单和标题栏
  拼出来。现在标题栏多一个格子图标：一份**只读**清单（等待回答 → 运行中 → 空闲，同组内最近活动的在前），
  每行写明窗口/会话名、状态、未读条数与多久之前，点一行把那个窗口提到前台。数据是主进程观察到的（流式开始/结束、
  待处理请求、未读计数），没有任何新信号、没有轮询 —— 变化时才推。
  另外：dock 的 pi 终端要等另一个 pi 装完包时，现在会**先说一句**（“终端要等另一个 pi 启动完成”）而不是静默不出现；
  对应地，e2e 改成等终端真的能输入（并把等待原因报出来），而不是把“还没就绪”当成缺陷。

### Fixed

- **点开对话里的一条链接，不会再把这个应用本身替换掉。** 对话渲染的是模型写出来的内容，markdown-it 会把裸 URL
  变成真的 `<a href>`，而此前没有任何东西拦着它 —— 点击即让**窗口自己**导航到那个远端页面。这不只是丢界面：
  Electron 的 preload 会在窗口的**每一次**导航后重新注入，所以那个远端源会拿到 `window.pi`，而它的白名单里就有
  `pi:term-input`（往活动终端写原始字节）与 `pi:fs-write`。CSP 拦不住这条路（`default-src` 不约束导航，
  `navigate-to` 从未在 Chromium 实现）。
  现在每个窗口只显示本应用自己装配的页面：`http(s)` / `mailto` 交给系统浏览器（这正是点链接的人期待的），
  `file:` 之类一律拒绝，且**不转交系统 shell** —— `pi:open-file` 本来就对路径做校验（禁可执行文件、禁越出
  工作区），把 `file:` 直接丢给系统等于绕开那道检查。判定本身是纯函数（15 条单测），接线只有两个监听器。
  测试用的 `PI_NAV_NO_OPEN=1` 让门禁能点真链接而不弹出浏览器。
- **日志里那条 `unknown msg type for renderer` 不再对每条正常消息都喊一遍。** 它原本挂在 `PI_DEBUG_WINDOW`
  判断的 else 分支上，于是**成功投递**的消息也被报成“未知类型”，正常使用时持续刷屏，把真正的问题埋掉
  （排查上一个问题时它就把测试输出整个淹了）。现在只对确实找不到通道的类型报警。
- 小补一处诊断：`PI_DEBUG_WINDOW=1` 现在也会标出窗口创建路径的四个点（子窗口创建 / 壳文件 / loadFile /
  子会话就绪），下次再遇到“子窗口空白”不必只靠猜。
- **无障碍门禁不再“跨主题”判色。** 它此前留下过一条 3:1 的缺陷记录，而那两次读数其实是把**浅色**调色板的
  文字色（`rgb(91,100,114)` / `rgb(28,32,39)`）配上了**暗色**主题的侧栏底色（`rgb(20,23,28)`）—— 正好是
  **3.10:1** 与 **1.11:1**，即主题切换中途取样的产物（该规则还带 `transition: color 130ms`，改完立刻读会
  拿到过渡**起点**）。现在门禁先断言“主题确实已经切到位”（`--pi-bg` 等于该主题的背景色）再判颜色，不达标时
  把 token 一并打出来。同一规则实测：暗色 **7.06:1**、浅色 **5.58:1**，`KNOWN-ISSUES` 那条已按测量假象结案。
- **agent 挂掉时，横幅会说**为什么**。** 此前只有一句 `Pi process exited (code 1). Click reload or send a
  message to restart.` —— 而 `code 1` 在 Windows 上恰好也是**强制结束**（应用自己关窗/重载走的是
  `taskkill /PID <pid> /T /F`）的样子，所以那个数字既不能行动、也不算证据；pi 真正的死因（例如某个 agent 包
  的 `npm install` 失败、配置读不了）只留在 `%TEMP%\pi-standalone-rpc.log` 里，而只有诊断包会提到它。
  现在 RPC 客户端留下 pi 最后的 6 行 stderr，横幅带上其中最后 3 行（单行截断 300 字）；9 条单测覆盖这些形状。
- **门禁不再把“窗口没在画”当成应用缺陷。** 两套 e2e 的抖动同源于此：Chromium 会节流被遮挡的窗口 —— 对比度门禁
  读到的是**冻结的过渡帧**（切主题必须通过设置窗口，它正好遮住聊天窗口）；daily 用 `innerText`（依赖布局）读同
  一个窗口，**连用户自己那条消息**都读成空，于是约一半运行误报“没有助手回复”（当时回合正在流着 288 个输出
  token）。现在：对比度测量先断言主题已切到位，读数取**级联值**（测量期间冻结过渡与动画），并正确处理
  `color(srgb …)` 与半透明背景的逐层合成；需要看渲染结果的小节先把窗口提到前台；daily 改读 `textContent`，
  并以**应用自己在写的会话文件**为地面真值区分“真回归”与“模型本就没输出文本”。`e2e:isolated` 连跑
  **5 次 0 失败**（116 项），daily **25/25**。
- **与会话有关的检查不再靠“碰巧有会话”。** 归档/删除小节会把沙箱里的会话清掉，多窗口检查此前便因此 skip；现在
  harness 按需**补种**自己那份会话再查。导出检查也不再以“可能是空会话”含糊跳过，而是报出真实原因
  （`Pi RPC process is not running` —— 这是应用支持的状态，重载即恢复；或恢复出来的会话确实没有消息）；
  “导出内容里含对话”的强断言移到 daily（那里刚跑完真回合），isolated 里改为等对话真的落地再断言。
- **两个 pi 不再同时往同一个 agent 包里装包（“反复退出 code 1”的那个真因）。** pi 每次启动都会按
  `~/.pi/agent/settings.json` 执行 `npm install --prefix <agentDir>/npm`，而应用会在多处同时起 pi（聊天会话、
  终端坞、第二个窗口）——**两个安装撞进同一个 prefix，在 Windows 上必输**：`npm warn tar TAR_ENTRY_ERROR
  ENOENT … lstat '…\node_modules\ajv\dist'` → `npm error code ENOTEMPTY … rmdir '…\node_modules\zod\src\v4\locales'`
  → pi 退出 1，而且输掉的那个 prefix 可能被留在半残状态，之后**每次启动都同样失败**。现在按 profile 串行化：
  同一 prefix 同一时刻只允许一个启动占用，占用的那次**报告就绪即交棒**。就绪信号不是猜的：实测 pi 在启动工作
  结束前**根本不读 stdin**（+1.0 s 写进去的请求，到 +70.9 s、最后一个 `npm install` 结束后才被回答），所以
  “pi 回答了应用发出的第一个请求”就是诚实的“已过启动”；没有包可装时它是 +1.0 s（而只等扩展自己发事件，在
  **一个包都没有的 profile 上会永远等不到** —— pi 什么都不打印）。占用最多 300 s（实测冷启动 +170 s 仍未就绪），
  超时才放行下一个 —— 早放行等于把竞态放回来，等待才是便宜的那个错。
- **pi 在启动阶段挂掉，现在会自动重开一次，而不是让你自己点重载。** 一次失败隔 1.5 s 重试一次，且**只在“尚未
  就绪”时重试**：跑起来之后再死不算启动失败（那是另一回事，重试只会掩盖它），第二次启动仍失败也照旧收手。
  失败两次后仍然保留原来的形状 —— **会话留在窗口里、横幅说明原因、发一条消息或重载即恢复**（这一条是测试逼出来
  的：原先那种直接抛出的写法会让窗口**连会话都没有**）。重试在**后台**跑，窗口不等它：会话在 pi 一被 spawn 就
  交给界面（装包中的 profile 一启动就是一分钟以上，等它就等于空窗）。同时修掉一个真缺陷：往刚死掉的 pi 写 stdin
  会在管道上抛 `EPIPE`，而 stdin 上**没有 error 监听** → 在 Electron 主进程里就是未捕获异常。
- **切工作副本不再等 pi 启动。** 面板里选另一个 worktree 时会重绑聊天会话（= 起一个 pi），而那条 IPC 此前是
  `await` 到底才回复的 —— 于是“选目录”要等新 pi 启动完（现在还要排队等另一个 pi 装完包）。工作目录本身在回复前
  就已保存，git 面板/文件树/终端各自跟着它走，所以现在**先回复、会话在后台重绑**（重绑完成后再弹 toast）。
  单测 235 条（含 14 条新的队列/启动集成测试，用一个不读 stdin 直到“装完”的假 pi 驱动真实的 spawn 路径）。

## 1.2.4 — 2026-09-19

### Added

- **图片与音频预览。** 文件面板里点开 PNG / JPG / GIF / WebP / BMP / ICO / AVIF / SVG 会直接显示图片（
  按比例缩放适配、不拉伸）；点开 MP3 / WAV / OGG / M4A / AAC / FLAC / OPUS 会显示带控件的播放器。
  上限 10 MB，走 `data:` URL，与背景图同一套做法 —— 页面的 CSP 本来就对 `img-src` / `media-src` 放行 `data:`，
  所以**没有放宽任何安全策略**。其余文件仍进原来的编辑器：判定只看扩展名，`logo.png.bak` 这种备份仍是文本。
- 顺带补上一处安全细节：媒体文件打开时 **`Ctrl+S` 不会把（空的）编辑器内容写回去** —— 键盘快捷键是全局的，
  它并不知道面板在显示什么。e2e 会校验 PNG 的字节在按下 `Ctrl+S` 之后**逐字节不变**。

### Fixed

- **git 面板上的「刷新」不刷新工作副本下拉。** 用 `git worktree add` 在应用运行期间新建的工作副本，
  要等到整页重新加载才会出现在下拉里 —— 对一个写着“刷新”的按钮来说这很奇怪。现在它会一并刷新。
- **编辑器与文件树的配色现在跟主题走。** CodeMirror 自带的配色是给白底设计的，在两套主题上都不可读
  （无障碍门禁量到：浅色下行号 **2.85:1**、暗色下字符串 **2.59:1**）。文件树的箭头/圆点列叠了 0.7 的透明度，
  于是 5.59:1 的颜色被合成为 **2.99:1**；错误横幅的红字压在自己的 14% 红底上只有 **4.0:1**。
  前者改用与其它界面相同的 token（调色板里没有可读色相可用时，层次改用字重表达），
  错误横幅保留红色底、文字改用正文色 —— 仍是一眼能认出的错误。两套主题现在都过 4.5:1 门禁。

## 1.2.3 — 2026-09-18

### Fixed

- **拖出来的子窗口空白：标题正确，里面没有对话。** 根因是 `preload` 的 `onMessage` 是**单监听器**：
  shim 持有它并把宿主的每条消息转发成 `window` 的 `message` 事件（vendored 聊天界面听的正是它），
  而精简外壳自己的标题脚本**在同一通道上又注册了一次**，把 shim 顶掉——于是界面一条消息也收不到，
  标题却照常更新（因为顶替者本人就是读 `sessionInfo` 的那一个），而且**全程不报错**。
  标题脚本改为监听转发出来的事件，与其他脚本和界面本身一致。
- 本版起**子窗口默认使用精简外壳**（只有拖拽标题栏 + 对话）；`PI_MINIMAL_CHILD=0` 可切回完整外壳做对照。
- 该缺陷的证明就是上个补丁加的那条断言：修复前精简外壳 FAIL，修复后 PASS（精简 87/0、完整 87/0）。

### Changed

- **字号设置现在实时生效**（1.2.3 实测）：聊天消息文字精确跟随「外观 → 字号」—— 设为 16 时真消息节点量到 **16px**，设为 22 时量到 **22px**，**运行中改也当场跟随**（e2e 已断言）。此前 token 只发给 `getAllWindows()` 看不到的 `<webview>`；而实时改字号还被上游启动时写下的一条**内联 `--chat-fs`** 压住（内联赢过所有样式表），现在由 shim 把它重新指向主控。
- **子窗口“偶尔空白”**：桥接修好后仍有一条竞态 —— 宿主的状态推送可能落在 app 挂载完成之前（实测：正常负载下 6→10 秒之间渲染，带诊断负载时 6 秒仍空）。`webviewReady` 现在在 600ms / 1.8s / 5s / 12s 再各重播一次（幂等、`unref`、会话销毁即放弃），子窗口内容稳定出现。
- 新增**默认关闭**的子窗口诊断开关：`PI_DEBUG_WINDOW=1` 打印子窗口的 console、preload 错误、加载失败、
  DOM 指纹与时间序列。它们不影响正常使用，只为下一次“需要看而不需要猜”的排查。

## 1.2.2 — 2026-09-15

### Fixed

- 拖出来的子窗口不会载入会话 ✗ → 已修 ✓。1.2.1 把子窗口换成「只有标题栏 + 对话」的精简外壳，跳过面板脚本之后会话就不再出现（窗口照开、标题正确、里面是空的）。已排除布局因素：三张面板样式表里影响全局或聊天区的选择器命中 0 条，所以问题在其中某条脚本做了什么，但在测出是哪一条之前，子窗口先用回完整外壳（已知能载入会话）。精简外壳仍留在代码里并被契约测试覆盖，定位后再接上。
- e2e 新增断言「子窗口真的显示出了对话」—— 原检查只验窗口存在、标题与位置，所以这个 bug 能逃过去。

## 1.2.1 — 2026-09-15

多窗口从「有一个右键菜单」变成真正可用，并补上「需要你确认」的提示音。

### Added

- **从会话列表拖出独立窗口** ✓：拖到窗口外松手即开窗 ✓，新窗口落在鼠标处 ✓（按所在显示器的工作区夹紧 ✓）。
  Electron 没有原生「把标签拖出成窗」的 API（electron#47854 仍未解决 ✓，macOS 的 tabbing API 也不适用 ✓），
  所以拖拽是自己实现的 HTML5 拖放 ✓。
- **操作提示音** ✓：任务结束（上行三音 ✓）与需要确权/提权（下行两音 ✓）各有辨识度 ✓。
  声音在隐藏渲染层用 Web Audio **现场合成** ✓（零音频素材 ✓）：主进程没有音频 API ✓、
  `Notification.sound` 仅 macOS 有效 ✓、Windows 专注助手会连系统提示音一起静音 ✓ —— 所以自己发 ✓。
  确权/提权即使窗口在前台也会响 ✓（回合被卡住等人 ✓），重复提醒只在该窗口仍在后台时发生 ✓、最多 3 次 ✓、
  应答/聚焦/切会话/关窗立即停止 ✓。
- **托盘窗口列表 / Ctrl+Shift+N / 启动恢复上次窗口** ✓（恢复可在设置里关掉 ✓）。

### Changed

- **拖出来的子窗口改成精简外壳** ✓：只有标题栏（会话名）与对话 ✓，没有侧栏、停靠面板、命令面板、令牌芯片 ✓
  —— 一个窗口 = 一个会话的观测台 ✓，主窗口仍是控制台 ✓。面板的脚本也一并移除 ✓：它们的轮询正是日后变成
  空闲 CPU 的东西 ✓，所以 `dom-contract` 现在断言它们**不存在** ✓。
- **子窗口上限 6 个** ✓：实测每个子窗口要独立一个 pi 进程（约 250 MB ✓）加一个渲染层（约 130 MB ✓），
  无上限就是换个名字的内存泄漏 ✓；超限时侧栏直接说明原因 ✓，而不是拖了个没有反应 ✓。

### Fixed

- **窗口标题永远是「Pi Heao GUI」** ✓ —— Electron 会把页面 `<title>` 覆盖到窗口标题上 ✓；
  子窗口现在使用会话名并屏蔽页面标题 ✓。
- **未读计数按窗口** ✓（原来是一个全局计数器 ✓，多窗口下失去意义 ✓）。
- 对比度门禁此前在聊天窗里调用 `pi:set-config` ✗ —— 那个通道被聊天窗 preload 挡着 ✓，
  现在改由设置窗切主题、聊天窗只负责测量 ✓。

### 说明（本轮实测）

- 空闲 CPU **0%** ✓、45 秒内存漂移 **+3.9 MB**（无泄漏 ✓）、单窗口空闲合计 **631 MB**，
  其中 pi 进程 **247 MB** ✓ —— 这也是「精简外壳让界面更干净 ✓、但省不了多少内存 ✓」这句结论的依据 ✓。
- 依赖漏洞 **0** ✓；Electron **43.7.0**（43 线最新 ✓）；本项目自身代码 **0 阻断错误** ✓。
- 每份生成的外壳文档约 5.7 MB ✓，现在**按配置复用一份** ✓，而不是每个窗口写一份 ✓。

## 1.2.0 — 2026-09-14

这一版全部围绕**界面的正确性**：浅色主题下读不清的字、写死的深色、只在启动时生效一次的外观设置。
把「外观」这一类 bug 的来源收敏成一条：**主题令牌里写死的深色值**（`!important` 把它们压在主题之上），
并给 e2e 加了一条**对比度断言**——它一能真正跑起来就抓出了三个真缺陷 ✓。

### Fixed

- **浅色主题读不清 ✓**：聊天里的标题/粗体曾是写死的 `#f2f4f8`（白底上 1.1:1 ✗），现在跟随主题；
  行内代码同理。这是「标题看不清」反复出现的原因 ✓。
- **代码块黑底 ✓**：`pre` 曾写死 `#12151a !important`，浅色下是黑块；现在走 `--pi-code-bg` ✓。
- **链接压不住 ✗ → ✓**：链接用的是**原始强调色**（3.20:1 ✓）；现在浅色主题取“变暗到可读”的那个色调（实测 **6.54:1** ✓）。
- **淡色文字 ✓**：`今天 / 刚刚` 等用的是 2.89:1 的灰；改为 `#5f6878`（过 4.5:1 ✓）。深色主题下顶栏
  「made by HEAOZIE」是 2.2:1 ✓、dock 的 meta 行是 3.50:1 ✓、空状态提示 `empty-hint` 是 4.31:1 ✓、
  dock 的删除计数 `pi-git-del` 是 4.20:1 ✓ —— 四处都改成跟随令牌 ✓（`empty-hint` 的真实原因是一条
  注入的 `!important` 规则压过了它自己的 `color` ✓，再加上 0.72 的 opacity ✓）。
- **选中文字可读 ✓**：选择背景原本用高饱和强调色压在白字上；现在用 `color-mix` 的低比例强调色 +
  主题正文色 ✓。
- **输入框双层框 ✓ / 双层焦点环 ✓**：`.composer` 与 `.composer-box` 都画了边框；现在只留一层 ✓。
- **实时改主题/强调色真的生效了 ✓**：启动后所有外观变更都落在一个被忽略的文档上（注入的令牌
  `<style>` 从没被替换过，而 pi-chat 也不监听主题消息）✓；现在由壳层就地替换该元素 ✓。
- **强调色「第二次改不生效」✓**：设置窗改外观时是「关掉再重开」，第二次改动被丢掉；现在**原地刷新** ✓
  （e2e：FAIL 2 → 0 ✓）。
- **顶栏会话名 ✓**：两条加载路径都会把标题卡在「加载中」；现在切换落定后镜像会话名，并做了
  try/catch 保护 ✓。
- **整段回复不再是点击区 ✓**：点正文会折叠/展开，选字也会被误触；现在正文不可点、光标为文字 ✓。
- **图标 ✓**：主窗、设置窗、聊天窗都换成 Pi 标记 ✓。

### Added

- **对比度断言（e2e）**：在浅色与深色下遍历页面所有元素，低于 4.5:1（大字/粗体、以及强调色按钮
  按 WCAG 1.4.11 取 3:1）即失败 ✓。它一能真正跑起来就指出了三处真缺陷：`.pi-tb-brand`
  （深色 2.2:1）、浅色 `span.empty-hint`（**4.31:1**）、深色 `span.pi-dock-meta`（**3.50:1**）✓。
  断言自身也修过一次：原来的「比值 × opacity」在数学上不成立 ✓，现在按有效透明度把文字色与背景
  **混合**后再算比值 ✓（hex 与 `rgb()` 两种写法都归一化 ✓）。
- **补齐 26 个 `--vscode-*` 变量桥接** ✓：被内置的聊天 UI 会读这些变量，缺一个就静默回退成默认值 ✓。

### Changed

- **回答默认展开 ✓**：长回答、长提问都默认展开，按钮变成「收起」✓。
- 统计摘要（turns/时长/+增/-删）改用 `textContent` 构建（该函数本来只产生纯文本）✓。
- `npm run test:daily`（发版前必跑的真窗口 + 真模型）默认改用托管模型 **opencode-go / mimo-v2.5** ✓ ——
  以前默认指向本地 llama-server ✓，服务器没开时整条门禁会失败 ✓，而那并不能说明应用有问题 ✓。
  本地模型仍可用环境变量指定：`PI_DAILY_PROVIDER=local-ornith-ud PI_DAILY_MODEL=ornith-1.5-9b-ud` ✓。

### Known issues

- **字号设置不会改变聊天文字大小 ✗** —— 根因已实测定位（层叠顺序：pi-chat 自己的 `<style>` 远在
  注入点之后，而 CSS 变量声明无法用 `!important` 压制），两条修法均已尝试并回退 ✓。
  实测数据与**下一步该做的测量**写在 [docs/KNOWN-ISSUES.md](docs/KNOWN-ISSUES.md) ✓。

## 1.1.3 — 2026-09-13

### Changed

- **那份 UI 代码现在归本项目所有**。以前 `vendor/upstream/` 是上游的逐字节副本，钉在 tag
  `v1.3.8`，并有一条 CI 作业会克隆上游、字节有差异就判失败 —— 保护了“聊天 UI 就是原版”
  这句承诺，代价是上游每次发版都可能让我们变红，而那个目录又写着“不许改”。
  现在：**代码是我们的** ✓，像 `src/` 一样可改，不要求对齐任何上游版本 ✓；
  目录改名为 **`studio/`**（“vendor”这个前缀已经不实 ✓）；当想要上游的新东西时，
  按 `docs/UPSTREAM.md` **先 diff、按需取，可以只取一部分** ✓。
- 因此 **CI 与构建不再访问上游仓库**：上游发新版**不会**让本项目变红 ✓。

### Added

- **DOM 契约测试**（`test/dom-contract.test.cjs`）：断言注入层依赖的钩子（`#input`、消息节点、
  我们自己的 `pi-*` 元素）仍在生成的页面里 ✓。因为现在两边都可能漂移 ✓，而“某个 id 被改名”的
  症状是**界面照常、功能静默消失** ✗ —— 本项目就咬过一次（一个 id 改名后 `npm run verify` 再也
  过不了 ✓）。它第一次运行就抓到了自己清单里的一个错钩子 ✓。

### Removed

- `scripts/check-upstream.cjs` 与 CI 里的 `upstream` 字节保真作业（被上面的新政策取代 ✓）。

## 1.1.2 — 2026-09-13

### Added

- **输入框粘贴可用 `Ctrl+Z` 一步撤回**。上游的粘贴处理器会拦掉浏览器默认行为、
  然后根据字符串重建编辑器内容，这次插入因此从不进入原生撤销栈 —— 逐字输入能撤，
  粘贴一大段却不能。现改为在捕获阶段接管粘贴，用 `document.execCommand('insertText')`
  插入：它是一次**原生编辑**，所以 Ctrl+Z 能一步吃掉整段，而且会触发 `input` 事件，
  上游的 `@路径` token 化、自适应高度、发送按钮状态等逻辑照常运行。
  **文件粘贴（图片/附件）不接管**，仍走上游路径；若原生编辑不可用，事件不会被拦下，
  交还原处理器，绝不吞掉粘贴。

### Fixed

- **扩展 UI 请求现在都会得到应答**。应用此前只应答交互型请求（`select` / `confirm` /
  `input` / `editor`，且要人点对话框），其余 `setWidget` / `setStatus` / `notify` /
  `setTitle` / `set_editor_text` **一律不应答**。而 pi 的扩展调用在宿主回复前不会结束，
  于是一轮里累计几十个悬住。修的时候有两个坑：`respondExtensionUi` 会把**没有 `value`
  的载荷变成 `cancelled`**（所以不能用空对象答应答），以及 `__mcp_status__` 分支会**提前
  return** 把应答整个跳过。

### Security

- CI 里的第三方 action 从可变标签（`@v4`）**固定到 commit SHA** —— 包括那个握着
  签名 token 的发布作业；Dependabot 仍会以可评审的 PR 推送升级。

## 1.1.1 — 2026-09-13

一次以安全审计为驱动的修复版；逐条细节见 [docs/release-notes-1.1.1.md](release-notes-1.1.1.md)。

### Fixed

- **主聊天窗口没有沙箱**（`sandbox: false`），而 README 与 SECURITY.md 都声称每个窗口
  都是 `sandbox: true` —— 偏偏渲染不可信 agent 输出的就是这个窗口。现已开启，并实测
  确认沙箱内 preload 仍能取到 `webUtils.getPathForFile`，拖拽不受影响。
- **文件面板可通过符号链接 / 目录联接逃出工作区**：路径校验只比对字符串，而 Windows 上
  建 junction 不需要管理员权限。现解析真实路径后再判断，无法验证时**拒绝而非放行**。
  **代价**：指向工作区外的链接不再出现在文件树里。
- **“用默认程序打开”的扩展名检查可被绕过**：`extname("payload.bat.")` 是 `.`、
  `extname("payload.bat ")` 是 `.bat`，两者都不在黑名单里。现先归一化路径再判断。
- **Dock 未转义就拼接 HTML**：文件名、git 路径与错误文本直接进 `innerHTML`。
- **配置与遥测文件是非原子写**：崩溃或被杀会留下截断文件（`config.json` 被截断
  等于丢失工作目录、主题、收藏与预算）。现改为写临时文件后 `rename`。
- **pi 输出单行无上限**：子进程若一直不发换行，主进程会持续累积到内存耗尽；
  现限制单行 32 MB，超限即中止连接并说明原因。
- **扩展挂载失败是静默的**：6 个内置扩展逐个 `existsSync`，缺了只是不加载，
  表现为“todo / 权限门 / rewind 凭空消失”。现会明确列出未挂载项。
- **Dock 重复注册 IPC 监听器**：每次终端重启都会再 `ipcRenderer.on` 一次。
- **`npm run verify` 永远失败**：它引用的 `pi-stat-cache` 已在源码中改名，
  因此无论代码好坏都必然报 null，而发布清单还把它列为门禁。已修正，33/33 全过。
- **SECURITY.md 的 CSP 段**补上 `script-src 'unsafe-inline'` 这一真实取舍，
  并去掉写死的过时版本号。

### Added

- **CI 会打包并校验产物**：此前无任何作业打包应用；新增阻断作业，读 `app.asar` 头部
  断言 13 条运行时路径（vendored 聊天 UI、bridge 扩展、node-pty 原生模块等）。
- **依赖监控**：Dependabot（分组）+ 咨询性 `npm audit --audit-level=high`。
- 单元测试 121 → **125**（新增符号链接 / junction 逃逸测试）。

### Changed

- 打包脚本显式加 `--publish never`：electron-builder 检测到 CI 会自动尝试发布
  到 GitHub，缺少 `GH_TOKEN` 时整个作业失败（本地不触发，故从未暴露）。

## 1.1.0 — 2026-09-13

### Added

- **自动检查更新**（`electron-updater`，指向本仓库 Release）：启动 20 秒后在后台检查，
  发现新版本自动下载，可在 **设置 → 诊断 → 版本与更新** 手动检查并一键重启安装。
  源码运行时不检查（会说明原因），`~/.pi/standalone/config.json` 里
  `autoCheckUpdates: false` 可关闭。**便携版不支持自更新**。
- **英文界面**覆盖到聊天窗标题栏、侧栏、停靠区（终端/文件/变更）、命令面板、
  统计面板、托盘菜单与关于对话框；在 **设置 → 常规 → 界面语言** 切换，默认 `跟随系统`。
  上游聊天区的文案由它自己的 locales 控制。
- **代码签名准备**：`signpath/artifact-configuration.xml` 与手动触发的
  `.github/workflows/sign-windows.yml`，走 SignPath Foundation 的开源项目免费签名
  （申请通过后配置 5 个 secrets/vars 即可用，见 `docs/RELEASING.md` 第 8 节）。

### Fixed

- **设置窗所有控件点不动**：内联脚本里被模板字面量吞掉的换行转义让字符串未闭合，
  整段脚本解析失败，因此没有任何事件处理器被注册。浏览器不报错、主进程也无日志，
  页面照常渲染。
- **命令面板（Ctrl+K）打不开**：`<style id="pi-palette">` 与面板根元素撞 id，
  `getElementById` 拿到的是样式元素，第一次按键走的是"关闭"分支。
- **标题栏两个指标不更新**：统计面板的指标 id 与标题栏芯片撞 id，值被写进了隐藏元素。
- **文件编辑器报错** `CodeMirror.defineSimpleMode is not a function`：Rust 模式依赖的
  simple-mode 插件未随包提供。
- **Windows 上更新日志的 npm 回退从未生效**：`npm.cmd` 直起被 Node 拒绝（EINVAL）。
- **终端重启后吞掉所有按键**：`termOpening` 未复位，且 ConPTY 被 kill 后需要沉降时间，
  否则新 shell 会在第一次写入时退出。
- 删除已被移动的会话时不再抛出裸 `ENOENT`，而是说明文件已被移动或删除。
- 命令面板在任意位置按 Esc 都能关闭（此前仅在输入框聚焦时有效）。
- 更新日志标签页打开即加载（此前需手动点"刷新"，看起来像空页）。
- 统计面板在会话尚未落盘时给出明确提示，而不是留空。

### Tests

这一版把"点不动"这类故障变成可自动发现的：

- 解析**每一个**注入脚本，并对生成页面做**重复 id 审计**（已反向验证：还原修复即报错）；
- preload 通道覆盖：每个窗口调用的通道必须在其白名单内，且每个授权都必须有处理器；
- 端到端逐页面逐功能：只读 73 项 + 沙箱破坏性 83 项，**任何渲染进程报错即失败**；
- i18n：残留中文清单、翻译后片段可解析、中文输出逐字节不变。

## 1.0.0 — 2026-09-13

首个面向"下载即用"的版本。

### Added

#### 聊天与会话

- 流式对话（上游 pi-chat UI）、模型与思考强度切换、fork/revert、`@file` 补全、
  Mermaid 与 KaTeX 渲染。
- 会话侧栏：新建、切换、重命名、置顶、删除、归档、恢复、拖放，以及完整键盘导航
  （方向键、Home/End、Enter、F2、Delete）。
- 多窗口：每个窗口拥有独立的 pi 进程与工作目录。
- 跨会话历史全文搜索，并跳转高亮命中消息。

#### 停靠区（终端 / 编辑器桥 / 提交流程）

- 终端：node-pty + ConPTY 真 PTY，xterm.js 渲染，默认运行 `pi` TUI（与聊天相同的
  bridge 扩展参数与 `--session`），可切换到系统 shell。
- 文件：工作区文件树 + CodeMirror 编辑器、保存、把选中内容发送到对话。
- 变更：git 分支与暂存状态，沿用上游提示词与逐文件 diff 截断来生成提交信息。

#### 遥测

- Token 与性能面板：首 token 延迟、解码耗时、tokens/秒、缓存命中率、推理占比、
  TTFT 的 p50/p95、逐轮表格、每日与每月花费及预算提醒（Ctrl+Shift+S）。
- 统计按会话持久化，重新打开会话时恢复。

#### 外壳与平台

- 命令面板（Ctrl+K）：命令、会话、斜杠指令、历史命中。
- 设置窗：主题（深/浅/跟随系统）与强调色、聊天字号、花费预算、开机自启、工作目录与
  最近目录、扩展包（`pi install` / `remove` / `list`）、技能增删改、提供方就绪检查
  （`pi auth check`）与一键登录、pi 更新日志、诊断面板（环境、RPC 日志、脱敏报告包）。
- 系统托盘：最近会话与未读计数、桌面通知、关闭到托盘。
- 内置差异窗口，由 rewind 扩展的快照驱动。
- 设置窗的界面语言（简体中文 / English）。
- 在便携版之外提供 NSIS 安装包。

### Changed

- 用设计令牌（`--pi-*`）取代零散配色，外壳、侧栏、设置与停靠区共用一套视觉系统。
- 会话列表改为异步 + 持久化元数据缓存（此前每次刷新同步读取约 170 MB，会卡住主进程）。
- API 密钥不再进入任何渲染进程：读取时脱敏，保存时还原。

### Fixed

- 聊天文档不再内嵌完整配置（其中可能含 `env` 里的密钥）。
- 主题与强调色无需重启即可生效；标题栏显示当前会话而不是一直空白。
- 注入样式改为包在 `<style>` 元素里 —— 裸文本会隐式闭合 `<head>`，让聊天窗渲染出一面
  CSS 文字墙。
- Windows 标题栏覆盖层不再遮住应用自己的标题栏按钮。
- `rewindDiff` 渲染在内置查看器里，而不是交给系统默认程序。
- ConPTY 需要绝对可执行路径；此前裸 `node` 导致终端失败，且错误被误报为缺少原生模块。
- `pi list` 的解析不再把无关的未缩进行当成包。

### Known limitations

- **未签名二进制**：Windows SmartScreen 首次运行会警告。
- **仅 Windows**：尚无 macOS/Linux 构建与测试。
- 界面语言当时只覆盖设置窗；本项目新增的聊天窗文案（停靠区、侧栏、命令面板、遥测面板）
  仍是中文。
- 无自动更新：需重新下载发布包升级。
- 上游插件的 LSP / 诊断集成依赖 VS Code，无法移植。
- 提供方 OAuth 在内置终端里由 pi 自己的 `/login` 完成，没有独立对话框。
- 每个窗口一个工作目录，而不是每会话（pi 没有 `set_cwd`）。
