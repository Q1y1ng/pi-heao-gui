# Roadmap / backlog

一份按**优先级与性价比**排序的待办清单，把两件已知缺陷和一轮同类产品对比得出的功能缺口放在一起。
这不是承诺的功能列表：每一项都写清**为什么值得做、怎么做、以及怎样算做完**，因为"做完"在这个仓库里
意味着**有测试**、而且**文档里的话都能被实测支撑**。

来源都标出来了：`KI` = 本文档同目录的 `KNOWN-ISSUES.md`，其余为同类项目的 README（在 §参考里列出）。

## 怎么排序

排序按 **性价比 = 用户价值 × 可验证性 ÷ 成本**，并对三类东西打折：

- **成本要用"带验证"来算**，不是"写完代码"。跨平台这种，成本主要不在写代码，而在三套系统上把终端、
  托盘、标题栏、自更新全部验一遍。
- **动到安全模型的项要降级**：凡是引入"渲染不可信远程内容"的面（内置浏览器就是这样），
  都要先设计边界，不能顺手加。
- **未知成本要先降成本**：先做一次决定性测量，再决定要不要排进去（字号那条就是标准例子）。

---

## P0 — 先做，成本小、价值高、且能立刻验证

| # | 事项 | 类型 | 为什么 | 成本 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 1 | **定位"精简子窗口不载入会话"的根因并接回** | 缺陷（`KI`） | 代码已写完、契约测试已覆盖，只差找出是**哪条被跳过的脚本**做了什么。接回后子窗口才真的是"一个窗口 = 一个会话" | 小：一次二分测量 + 接线 | e2e 的 *"the session window actually shows the conversation"* 在子窗口为精简外壳时通过；`dom-contract` 的最小形态断言仍通过 |
| 2 | **Git worktree 切换** | 功能缺口（abcwyc 有） | 同一仓库多分支并行是 pi 的重度用法；我们已有 git 面板、`git.ts` 与分支显示，缺的只是选择与切换 | 小：一个下拉 + 一次 `git worktree` 调用 + 面板刷新 | 单测覆盖 worktree 列表/切换的解析；e2e 在临时仓库上建两个 worktree 并切换成功；换目录后会话的 `cwd` 与面板一致 |
| 3 | **字号设置不生效** | 缺陷（`KI`，1.2.0 起） | 外观设置里唯一一个坏掉的开关；两次修法分别弄崩过界面与测试 | **未知 → 先测**：给 SHIM 的消息回调加 `console.log`，抓聊天窗的 renderer console，看清启动路径与实时路径各自跑了哪几行 | 实测记录写进 `KI`；修好后 e2e 断言 `--chat-fs` 与消息文字字号都跟随设置变化 |

## P1 — 价值高，但比 P0 重一些

| # | 事项 | 类型 | 为什么 | 成本 | 验收标准 |
| --- | --- | --- | --- | --- | --- |
| 4 | **图片与音频预览** | 功能缺口（abcwyc 有） | 目前只能"用系统程序打开"；图片与音频在应用内显示的成本很低（不需要新依赖） | 小到中 | e2e：打开一张 PNG 与一个音频文件，面板内出现对应元素；非图片/音频仍走原有编辑器路径 |
| 5 | **多项目工作区** | 功能缺口（yuguo1983、DLYZZT） | 现在每个窗口一个 `workspaceRoot`，靠多窗口勉强达到；项目列表、搜索、拖动排序、项目间隔离才是正向做法 | 中：项目存储 + 侧栏分组 + 每项目独立 `cwd` | 单测覆盖项目存储与去重；e2e 添加两个项目并切换，断言会话列表与终端 `cwd` 都跟着换 |
| 6 | **待决策事项的汇总面板** | 功能缺口（quntion 的"可视化审批"） | `dialog` 已经是"有人在做决定"的唯一信号，而且我们已经有提示音；缺的是"待我处理"的清单，能把提示音闭环成工作流 | 中：收集未应答的 dialog + 队列 UI + 一键跳转 | e2e：制造一个待确认请求，面板出现条目、点击后跳回对应会话窗口；应答后条目消失 |
| 7 | **PDF / DOCX 预览** | 功能缺口（abcwyc 有） | 读文档时不必离开应用 | **大**：PDF 需要 pdf.js（体积），DOCX 需要转换 | 单独评估：先量化加载体积与内存增量，再决定做不做 |

## P2 — 有价值，但要么重、要么动安全边界

| # | 事项 | 为什么排后面 |
| --- | --- | --- |
| 8 | 运行中断恢复（重开时接着跑） | 需要把 pi 的进行中状态落盘，并处理"恢复时上游已变"的冲突；价值中 |
| 9 | 自定义主题文件 / 主题市场 | 我们已有主题 + 任意强调色；再加一层文件格式与校验，收益主要是外观向 |
| 10 | 窗口内标签页（同窗多会话） | 与现有的"拖出成窗 + 窗口上限 + 会话独占写者"模型冲突，需要重新设计与仲裁 |
| 11 | Agent 舰队 / 受管进程面板 | 属于"多 agent 编排"，与 pi 自身的能力边界重叠，先看清 pi 的规划再动 |
| 12 | 内置浏览器 / 共享浏览器 | **动安全模型**：等于新增"渲染不可信远程内容"的面。必须单独设计边界（沙箱、CSP、权限、域名白名单）后才有资格排队 |
| 13 | 消息渠道（Telegram / 微信 / 飞书） | 每接入一个渠道都要凭据管理、权限模型、失败可见性；价值对特定用户很高，对其他用户为零 |

## 单独立项 — 跨平台（macOS / Linux）

**这是最大的一条缺口，也是本机价值最低的一条**（作者与主要使用者都在 Windows）。它应当单独排期，因为：

- 成本主要不在代码：`titleBarOverlay`、`node-pty` 预编译与 conpty、托盘图标、自更新、深链，
  每一样都要在另外两套系统上**真机验证**；
- 仓库的既有习惯是"打包后真的启动一次"（1.2.x 的发布说明里就有这一步），跨平台意味着这条验证要 ×3；
- 在没做之前，README 与发布说明**不应暗示**支持 mac/linux（现在没有暗示，保持这样）。

**建议**：先做一次可行性测量（能否在 mac/linux 上跑通 `npm test` + `e2e:isolated`），再决定排期。

---

## 明确不做（现在）

- **把 UI 换成另一个框架重写**：现有外壳的问题都是可定位的具体缺陷，重写换不来这些缺陷的解决。
- **在应用里内建 pi 的能力**（模型路由、工具编排）：边界很清楚 —— 能力由 pi 提供，我们负责宿主。
- **为"看起来功能多"而加的面板**：每加一个面板就多一份轮询、多一块内存，1.2.1 已经吃过一次这个亏
  （见 `KI` 的精简外壳条目）。

## 每个条目的通用完工标准

1. **有测试**：纯逻辑进 `test/*.test.cjs`；用户可见行为进 `scripts/e2e.cjs` 的一个 section。
2. **有实测**：性能/资源相关的改动要给出前后数字（1.2.x 的记录方式可以照抄）。
3. **文档同步**：`CHANGELOG.md` 写用户能感知的变化；`README` 的话必须能被实测支撑；
   没修好的进 `KI`，并写清**已排除什么**与**下一步测什么**。
4. **版本**：功能与修复走补丁位；小版本号留给重大更新（见 `docs/RELEASING.md`）。

## 参考

对比样本（同类的 pi 桌面客户端，均为公开仓库）：`sjdhome/backpressure`、
`abcwyc/pi-agent-desktop`、`yuguo1983/pi-desktop`、`JustGenius-s/e-pi`、`quntion/pi-desktop`、
`DLYZZT/pi-desktop`、`minghinmatthewlam/pi-gui`、`psylsph/pi-desktop`、`justhil/pi-app`、
`Chasen-Liao/pi-agent-desktop`。上游：`earendil-works/pi`；VS Code 前端：`pi-agent-studio`。
